(ns antoine.sql.numeradores
  (:require [antoine.servicios.conexiones :refer [consulta-parametros]]
            [honey.sql :as sql]))

(defn obtener-nro-protocolo
  []
  (let [transaccion (sql/format)]))